CREATE TABLE users (id serial PRIMARY KEY, username VARCHAR (20), firstname VARCHAR (24), lastname VARCHAR (24), email VARCHAR(320), passkey VARCHAR(40));

/* Run in both databases - makersbnb and makersbnb_test ^^^